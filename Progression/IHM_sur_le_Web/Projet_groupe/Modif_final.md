# Modif final

- `id="theme-button"` → `id="btn-theme"` (script mode sombre)
- `id="rechercheTexte"` → `id="search"` (script filtrage)
- `id="tri-critere"` → `id="sort-key"` et `id="tri-ordre"` → `id="btn-sort-order"` (script tri)
- `.personne-card` → `.person-card` + ajouter `data-name` et `data-domain` sur chaque carte (script tri + filtrage)
- `data-cible` → `data-target` sur les boutons d’onglets (script tabs)
- `id="toggle-bio"` → `id="btn-toggle-bio"` et `id="plus-bio"` → `id="bio-plus"` (script voir plus)
- `id="top-button"` → `id="btn-top"` (script retour en haut)