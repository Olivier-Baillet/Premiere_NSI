# TP papier :

### Exo 1

Combien de fois s’exécute le corps de chacune des deux boucles ci-dessous, et quelle est la valeur de `S` à la fin de l’exécution ?

```python
S = 0
while S < 20:
	S = S + 5 
```

```python
S = 1
while S != 100:
	S = S * 2
```

---

```python
c = 0
while S <= 3:
	print("ok")
	S += 1
```

```python
S = 0
while S > 3:
	print("ok")
	S += 1
print("fini")
```

### Exo 2

Compléter la condition ci-dessous qui permet de vérifier que la variable est pair (qu’importe la valeur de `a`).

```python
a = 10
if a … :
  print(a,” est pair”)
```

---

### Exo 3

Ecrire avec une boucle `while` un algorithme qui affiche les nombres pairs entre 2 et 20.

---

### Exo 4

Pour transporter des poteaux sur une île, on dispose d’une barge dont la masse maximal est de 1800 kg.
Écrire un programme qui demande à l’utilisateur la masse d’un poteau puis le nombre de poteaux et qui affiche le message « Risque de surcharge! » si la masse utile est dépassée.

---